spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/ticketing_test
    username: ${DB_USERNAME:ticketing_app}
    password: ${DB_PASSWORD:}

  flyway:
    clean-disabled: false # tests wipe and rebuild ticketing_test every run (ADR-0010)
